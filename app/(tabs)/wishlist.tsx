import Container from "@/components/Container";
import { View, Text } from "react-native";

const Wishlist = () => {
  return (
    <Container>
      <Text>Wishlist</Text>
    </Container>
  );
};

export default Wishlist;
